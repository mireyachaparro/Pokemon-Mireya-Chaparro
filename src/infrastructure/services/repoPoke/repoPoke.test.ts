import { fetchPokemons } from "./pokeRepository";

describe("Given fetchPokemons repository", () => {
    describe("when we instanciate it", () => {
        test("then it should bring the pokemons", () => {
            //
        });
    });
});
